{
  "name": "gearbook-explorer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:data": "tsx scripts/build-data.ts",
    "dev": "npm run build:data && vite",
    "build": "npm run build:data && tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  }
}
